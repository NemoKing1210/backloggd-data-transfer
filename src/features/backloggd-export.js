/**
 * Future: export the current user's Backloggd library into a transfer document.
 * Placeholder so the destination/source split stays clear from day one.
 *
 * @returns {Promise<import('../../format/schema.js').TransferDocument | null>}
 */
export async function exportBackloggdLibrary() {
  console.warn('[bdt] exportBackloggdLibrary is not implemented yet');
  return null;
}
